/**
 * @file
 * Dummy JS file for ESLint testing purposes.
 *
 * This file exists solely to provide ESLint with content to validate
 * during CI testing. It prevents errors when no
 * JS files are found in the module.
 *
 * This ensures the ESLint job runs successfully and validates the
 * CI pipeline's JS linting capabilities.
 */

// A simple variable declaration to ensure valid JS syntax.
const dummyVariable = true;
